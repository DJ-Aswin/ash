# ⚙ Installation Guide

Follow the steps below to set up the *Student Management System* on your system.

---

## 🧰 Prerequisites
Before installation, make sure you have the following installed:
- *Python 3.10+*
- *pip* (Python package manager)
- *Git* (for cloning the repository)

To verify your setup:
```bash
python --version
pip --version
git --version
